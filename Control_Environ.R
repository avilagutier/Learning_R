#Comentario individual
"
Forma de comentarios multilinea
"
"
En estudios de estadística se llevaran a cabo varias modificaciones
se necesitara mostrar el contenido de objetos. 
Para mostrar la valor de un objeto x se hace por el comando 
print
"
u = 6
print(u)

objects() #Da a conocer los objetos que se tienen en la sesion

ls()# igual que objects

#para eliminar uno se emplea la función rm: 
rm(u)
objects()

